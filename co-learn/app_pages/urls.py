from django.urls import path
from . import views

urlpatterns = [
    # path("", views.HomePageView.as_view(), name="home_page_url"),
    # path("about/", views.AboutPageView.as_view(), name="about_page_url")
]
